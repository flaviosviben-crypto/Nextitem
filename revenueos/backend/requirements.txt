# Version floors are the versions this codebase is actually tested against.
# Ceilings guard the next major, where pandas in particular changes semantics
# that this code depends on (NaN handling, replace(), resample()).
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.10,<3
python-multipart>=0.0.20
pandas>=2.2,<4
numpy>=1.26,<3
duckdb>=1.1,<2
anthropic>=0.42,<1
python-dateutil>=2.9

# development
pytest>=8.3
httpx>=0.28
