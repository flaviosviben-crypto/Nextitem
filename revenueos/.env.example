# Server-side only. Enables the conversational AI Analyst and AI narratives.
# Everything else (all analytics, matching, opportunities) works without it.
ANTHROPIC_API_KEY=

# Where the frontend proxies /api requests.
BACKEND_URL=http://backend:8000

# Comma-separated origins allowed to call the API directly.
CORS_ORIGINS=http://localhost:3000
